<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) traffic for local development -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android Emulator localhost -->
        <domain includeSubdomains="true">10.0.2.2</domain>

        <!-- Physical Device - Your PC's IP Address -->
        <domain includeSubdomains="true">192.168.0.106</domain>

        <!-- Other local network IPs (if needed) -->
        <!-- <domain includeSubdomains="true">192.168.0.218</domain> -->
        <!-- <domain includeSubdomains="true">192.168.1.100</domain> -->
    </domain-config>

    <!-- For production, you can add specific HTTPS-only domains -->
    <!-- <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.yourproduction.com</domain>
    </domain-config> -->
</network-security-config>

