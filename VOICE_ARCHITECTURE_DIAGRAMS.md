# 🎤 Voice Integration Architecture & Flow Diagrams

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │         VoiceGeminiChatWindow Component                    │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐          │  │
│  │  │   Header   │  │  Messages  │  │   Input    │          │  │
│  │  │  Language  │  │   History  │  │  🎤 + 💬   │          │  │
│  │  │  Settings  │  │  Display   │  │  Controls  │          │  │
│  │  └────────────┘  └────────────┘  └────────────┘          │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    ▼                   ▼
          ┌──────────────────┐  ┌──────────────────┐
          │ useSpeechToText  │  │ useTextToSpeech  │
          │     Hook         │  │     Hook         │
          └──────────────────┘  └──────────────────┘
                    │                   │
          ┌─────────┴─────────┐        │
          ▼                   ▼        ▼
┌──────────────────┐  ┌──────────────────────────┐
│ SpeechRecognition│  │   SpeechSynthesis API    │
│   (Browser API)  │  │     (Browser API)        │
│   Voice → Text   │  │     Text → Voice         │
└──────────────────┘  └──────────────────────────┘
          │
          │ Text transcript
          ▼
┌─────────────────────────────────────────────────┐
│            useGemini Hook                       │
│  (Existing - No changes required)               │
└─────────────────────────────────────────────────┘
          │
          │ API Request
          ▼
┌─────────────────────────────────────────────────┐
│       Django Backend (Unchanged)                │
│  ┌───────────────────────────────────────────┐  │
│  │    GeminiViewSet.chat()                   │  │
│  │         ↓                                 │  │
│  │    GeminiService.chat_stream()            │  │
│  │         ↓                                 │  │
│  │    Gemini AI + MCP Tools                  │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
          │
          │ Response (streamed)
          ▼
┌─────────────────────────────────────────────────┐
│        Message Display in UI                    │
└─────────────────────────────────────────────────┘
          │
          │ Auto-speak response
          ▼
┌─────────────────────────────────────────────────┐
│    useTextToSpeech (speaks response)            │
└─────────────────────────────────────────────────┘
```

---

## 🔄 User Interaction Flow

### Voice Input Flow (Speech-to-Text)

```
User clicks 🎤 button
          │
          ▼
┌─────────────────────┐
│ startListening()    │ ← useSpeechToText hook
└─────────────────────┘
          │
          ▼
┌─────────────────────┐
│ Browser asks for    │
│ microphone access   │
│   [Allow] [Block]   │
└─────────────────────┘
          │ (if allowed)
          ▼
┌─────────────────────┐
│ Show "Listening..."  │ ← Visual feedback
│ indicator            │
└─────────────────────┘
          │
          ▼
User speaks: "Show my customers"
          │
          ▼
┌─────────────────────────────────┐
│ SpeechRecognition API           │
│ (Google servers in background)  │
└─────────────────────────────────┘
          │
          ├─► Interim results: "Show..." → Display in input
          ├─► Interim results: "Show my..." → Update input
          └─► Final result: "Show my customers"
                      │
                      ▼
          ┌─────────────────────┐
          │ onTranscript()      │ ← Callback fired
          │ "Show my customers" │
          └─────────────────────┘
                      │
                      ▼
          ┌─────────────────────┐
          │ sendMessage()       │ ← Auto-send
          └─────────────────────┘
                      │
                      ▼
          ┌─────────────────────┐
          │ Backend API call    │
          └─────────────────────┘
```

---

### Voice Output Flow (Text-to-Speech)

```
Bot response received
          │
          ▼
┌───────────────────────────────┐
│ Message added to UI           │
│ "You have 47 customers..."    │
└───────────────────────────────┘
          │
          ▼
┌───────────────────────────────┐
│ useEffect detects new message │
│ (if role === 'assistant')     │
└───────────────────────────────┘
          │
          ▼
┌───────────────────────────────┐
│ Check autoSpeak enabled?      │
└───────────────────────────────┘
          │ (if yes)
          ▼
┌───────────────────────────────┐
│ speak(message.content)        │ ← useTextToSpeech hook
└───────────────────────────────┘
          │
          ▼
┌───────────────────────────────┐
│ SpeechSynthesisUtterance      │
│ created with text             │
└───────────────────────────────┘
          │
          ▼
┌───────────────────────────────┐
│ Set language, rate, pitch     │
│ Select voice for language     │
└───────────────────────────────┘
          │
          ▼
┌───────────────────────────────┐
│ window.speechSynthesis.speak()│
└───────────────────────────────┘
          │
          ▼
┌───────────────────────────────┐
│ Show "🔊 Speaking" badge      │ ← Visual feedback
└───────────────────────────────┘
          │
          ▼
User hears: "You have 47 customers..."
          │
          ▼
┌───────────────────────────────┐
│ Speech completes              │
│ onEnd() callback fires        │
└───────────────────────────────┘
          │
          ▼
┌───────────────────────────────┐
│ Remove "Speaking" badge       │
└───────────────────────────────┘
```

---

## 🔀 Complete Conversation Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    USER STARTS INTERACTION                  │
└─────────────────────────────────────────────────────────────┘
                            │
              ┌─────────────┴─────────────┐
              ▼                           ▼
    ┌──────────────────┐        ┌──────────────────┐
    │ Click 🎤 button  │   OR   │ Type in textbox  │
    └──────────────────┘        └──────────────────┘
              │                           │
              ▼                           │
    ┌──────────────────┐                 │
    │ Allow microphone │                 │
    └──────────────────┘                 │
              │                           │
              ▼                           │
    ┌──────────────────┐                 │
    │ User speaks      │                 │
    │ "Show my leads"  │                 │
    └──────────────────┘                 │
              │                           │
              ▼                           │
    ┌──────────────────┐                 │
    │ Voice → Text     │                 │
    │ "Show my leads"  │                 │
    └──────────────────┘                 │
              │                           │
              └───────────┬───────────────┘
                          ▼
              ┌────────────────────────┐
              │ sendMessage()          │
              │ "Show my leads"        │
              └────────────────────────┘
                          │
                          ▼
              ┌────────────────────────┐
              │ POST /api/gemini/chat/ │
              └────────────────────────┘
                          │
                          ▼
              ┌────────────────────────┐
              │ Django Backend         │
              │ GeminiService          │
              └────────────────────────┘
                          │
                          ▼
              ┌────────────────────────┐
              │ Gemini AI + MCP Tools  │
              │ Query database         │
              │ Format response        │
              └────────────────────────┘
                          │
                          ▼
              ┌────────────────────────┐
              │ Stream response back   │
              │ "You have 23 leads..." │
              └────────────────────────┘
                          │
                          ▼
              ┌────────────────────────┐
              │ Display in chat UI     │
              │ Show bot message       │
              └────────────────────────┘
                          │
                          ▼
              ┌────────────────────────┐
              │ Auto-speak enabled?    │
              └────────────────────────┘
                          │ (if yes)
                          ▼
              ┌────────────────────────┐
              │ Text → Voice           │
              │ speak("You have 23...") │
              └────────────────────────┘
                          │
                          ▼
              ┌────────────────────────┐
              │ User hears response    │
              │ 🔊 "You have 23 leads" │
              └────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                 INTERACTION COMPLETE                        │
│         User can speak again or type new message            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🧩 Component Hierarchy

```
VoiceGeminiChatWindow
│
├─ Header Section
│  ├─ Title ("Voice AI Assistant")
│  ├─ Status Badge (Online/Offline)
│  ├─ Recording Badge (if listening)
│  ├─ Speaking Badge (if speaking)
│  ├─ Language Selector
│  ├─ Auto-speak Toggle Button
│  └─ Clear Chat Button
│
├─ Browser Support Warnings
│  ├─ Voice Input Not Supported Alert
│  └─ Voice Output Not Supported Alert
│
├─ Messages Area
│  ├─ Status Alerts (if API unavailable)
│  ├─ Error Alerts (if errors)
│  ├─ Welcome Screen (if no messages)
│  │  ├─ Welcome Icon
│  │  ├─ Instructions
│  │  └─ Quick Start Prompts
│  │
│  └─ Message List
│     └─ For each message:
│        ├─ Role Label (User/AI Assistant)
│        ├─ Message Bubble
│        │  ├─ Content
│        │  ├─ Timestamp
│        │  └─ Speak Button (if assistant)
│        └─ Speaking Badge (if active)
│
└─ Input Area
   ├─ Recording Indicator (if listening)
   ├─ Input Controls Row
   │  ├─ Microphone Button (🎤/⏹️)
   │  ├─ Textarea (with placeholder)
   │  └─ Send Button
   └─ Status Bar
      ├─ "AI is typing..." (if streaming)
      └─ Help Text (keyboard shortcuts)
```

---

## 🔌 Hook Dependencies

```
VoiceGeminiChatWindow
│
├─ useGemini()
│  ├─ messages
│  ├─ isLoading
│  ├─ isStreaming
│  ├─ error
│  ├─ status
│  ├─ sendMessage()
│  └─ clearMessages()
│
├─ useSpeechToText()
│  ├─ isListening
│  ├─ transcript
│  ├─ finalTranscript
│  ├─ interimTranscript
│  ├─ isSupported
│  ├─ error
│  ├─ startListening()
│  ├─ stopListening()
│  ├─ resetTranscript()
│  └─ setLanguage()
│
└─ useTextToSpeech()
   ├─ isSpeaking
   ├─ isSupported
   ├─ voices
   ├─ selectedVoice
   ├─ error
   ├─ speak()
   ├─ cancel()
   ├─ pause()
   ├─ resume()
   ├─ setVoice()
   ├─ setLanguage()
   ├─ setRate()
   ├─ setPitch()
   └─ setVolume()
```

---

## 📡 API Communication Flow

```
Frontend                          Backend
────────                          ────────

User speaks                         
  │
  ▼
SpeechRecognition
  │ (Google servers)
  ▼
Text transcript
  │
  ▼
POST /api/gemini/chat/  ────────►  Django receives
{                                   │
  message: "Show leads",            ▼
  history: [...]               GeminiViewSet.chat()
}                                   │
                                    ▼
                              Authenticate user
                                    │
                                    ▼
                              GeminiService.chat_stream()
                                    │
                                    ▼
                              Build user context
                                    │
                                    ▼
                              Create MCP tools
                                    │
                                    ▼
                              Call Gemini AI
                                    │
                              (may use MCP tools)
                                    │
                                    ▼
Response stream          ◄────── Stream response
  │                              (Server-Sent Events)
  ▼
Parse SSE events
  │
  ├─► data: {"type": "message", "content": "You"}
  ├─► data: {"type": "message", "content": " have"}
  ├─► data: {"type": "message", "content": " 23 leads"}
  └─► data: {"type": "completed"}
  │
  ▼
Update UI in real-time
  │
  ▼
Message complete
  │
  ▼
Auto-speak response
  │
  ▼
SpeechSynthesis
  │ (browser native)
  ▼
User hears response
```

---

## 🔄 State Management

```
Component State Flow:

┌────────────────────────────────────────┐
│   VoiceGeminiChatWindow State          │
├────────────────────────────────────────┤
│ - input: string                        │
│ - selectedLanguage: string             │
│ - autoSpeakEnabled: boolean            │
│ - showSettings: boolean                │
│ - lastSpokenMessageId: string | null   │
└────────────────────────────────────────┘
                │
                ├─► Updates when user types
                ├─► Updates when language changes
                ├─► Updates when toggle clicked
                └─► Updates when message spoken
                
┌────────────────────────────────────────┐
│   useGemini Hook State                 │
├────────────────────────────────────────┤
│ - messages: GeminiMessage[]            │
│ - isLoading: boolean                   │
│ - isStreaming: boolean                 │
│ - error: string | null                 │
│ - status: GeminiStatusResponse | null  │
└────────────────────────────────────────┘
                │
                ├─► Updates when message sent
                ├─► Updates when response streams
                └─► Updates on error
                
┌────────────────────────────────────────┐
│   useSpeechToText Hook State           │
├────────────────────────────────────────┤
│ - isListening: boolean                 │
│ - transcript: string                   │
│ - finalTranscript: string              │
│ - interimTranscript: string            │
│ - error: string | null                 │
└────────────────────────────────────────┘
                │
                ├─► Updates when listening starts/stops
                ├─► Updates on every speech result
                └─► Updates on error
                
┌────────────────────────────────────────┐
│   useTextToSpeech Hook State           │
├────────────────────────────────────────┤
│ - isSpeaking: boolean                  │
│ - voices: SpeechSynthesisVoice[]       │
│ - selectedVoice: SpeechSynthesisVoice  │
│ - error: string | null                 │
└────────────────────────────────────────┘
                │
                ├─► Updates when speaking starts/stops
                ├─► Updates when voices loaded
                └─► Updates on error
```

---

## 🎨 UI State Diagram

```
┌─────────────────┐
│  Initial State  │
│  - No messages  │
│  - Not listening│
│  - Not speaking │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────┐
│   User Clicks Mic Button    │
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   Permission Check          │
│   - First time: Show prompt │
│   - Already allowed: Start  │
│   - Denied: Show error      │
└─────────────────────────────┘
         │ (if allowed)
         ▼
┌─────────────────────────────┐
│   Listening State           │
│   - Show recording badge    │
│   - Show "Listening..." text│
│   - Disable text input      │
│   - Change mic button (red) │
│   - Update input with       │
│     interim transcript      │
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   Speech Complete           │
│   - Final transcript ready  │
│   - Auto-send message       │
│   - Reset input             │
│   - Stop listening          │
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   Loading State             │
│   - Show spinner            │
│   - Disable inputs          │
│   - "AI is typing..."       │
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   Response Streaming        │
│   - Show partial response   │
│   - Cursor blinking         │
│   - Keep loading state      │
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   Response Complete         │
│   - Full message displayed  │
│   - Add to message history  │
│   - Ready for next input    │
└─────────────────────────────┘
         │ (if autoSpeak enabled)
         ▼
┌─────────────────────────────┐
│   Speaking State            │
│   - Show "Speaking" badge   │
│   - Highlight message       │
│   - User hears response     │
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   Ready for Next Message    │
│   - All states reset        │
│   - User can speak or type  │
└─────────────────────────────┘
         │
         └──► Loop back to "User Clicks Mic Button"
```

---

## 🔧 Error Handling Flow

```
Error Occurs
    │
    ├─► SpeechRecognition Error
    │   │
    │   ├─► "not-allowed"
    │   │   └─► Show: "Please enable microphone access"
    │   │
    │   ├─► "no-speech"
    │   │   └─► Show: "No speech detected. Please try again"
    │   │
    │   ├─► "audio-capture"
    │   │   └─► Show: "Microphone not found"
    │   │
    │   ├─► "network"
    │   │   └─► Show: "Network error. Check connection"
    │   │
    │   └─► Other errors
    │       └─► Show: Generic error + fallback to text
    │
    ├─► SpeechSynthesis Error
    │   │
    │   ├─► Voice not available
    │   │   └─► Use default voice
    │   │
    │   ├─► Synthesis failed
    │   │   └─► Show: "Could not speak message"
    │   │
    │   └─► Other errors
    │       └─► Show: Generic error + continue silently
    │
    └─► API/Backend Error
        │
        ├─► Network error
        │   └─► Show: "Cannot connect to server"
        │
        ├─► Authentication error
        │   └─► Show: "Please log in again"
        │
        ├─► Gemini API error
        │   └─► Show: "AI service temporarily unavailable"
        │
        └─► Other errors
            └─► Show: Error message from backend
```

---

## 📊 Performance Optimization Points

```
┌─────────────────────────────────────┐
│   Optimization Strategy             │
├─────────────────────────────────────┤
│                                     │
│ 1. Component Level                  │
│    - React.memo() for messages      │
│    - useMemo() for processed data   │
│    - useCallback() for handlers     │
│                                     │
│ 2. State Updates                    │
│    - Batch state updates            │
│    - Debounce interim transcripts   │
│    - Throttle scroll events         │
│                                     │
│ 3. Network                          │
│    - Stream responses (don't wait)  │
│    - Cancel previous requests       │
│    - Implement request queue        │
│                                     │
│ 4. Voice APIs                       │
│    - Clean up on unmount            │
│    - Cancel speech on navigation    │
│    - Reuse recognition instance     │
│                                     │
│ 5. Rendering                        │
│    - Virtual scrolling for messages │
│    - Lazy load voice component      │
│    - Code splitting                 │
│                                     │
└─────────────────────────────────────┘
```

---

## 🎯 Key Integration Points

```
Your Existing Code                Voice Integration
─────────────────                ─────────────────

GeminiChatWindow    ──────►  VoiceGeminiChatWindow
                             (extends with voice)

useGemini hook      ──────►  useGemini hook
(unchanged)                  (no changes)
      │                            │
      │                            ├─► useSpeechToText
      │                            │   (voice input)
      │                            │
      │                            └─► useTextToSpeech
      │                                (voice output)
      ▼                            ▼
gemini.service.ts   ──────►  gemini.service.ts
(unchanged)                  (no changes)
      │                            │
      ▼                            ▼
Django Backend      ──────►  Django Backend
(unchanged)                  (no changes)
```

---

## 🎨 Visual Component Structure

```
┌────────────────────────────────────────────────────────────┐
│  VoiceGeminiChatWindow                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Header                                              │  │
│  │  🤖 Voice AI Assistant  ● Online  🎤 Recording       │  │
│  │  [Language: 🇺🇸 English] [🔊] [🔄 Clear]            │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Messages Scroll Area                                │  │
│  │                                                       │  │
│  │  ┌──────────────────────────────────────┐            │  │
│  │  │ User Message (Right-aligned, purple) │            │  │
│  │  │ "Show me my customer statistics"     │            │  │
│  │  │ 10:30 AM                             │            │  │
│  │  └──────────────────────────────────────┘            │  │
│  │                                                       │  │
│  │  ┌──────────────────────────────────────┐            │  │
│  │  │ 🤖 AI Assistant (Left-aligned, gray)  │ [🔊]       │  │
│  │  │ "You have 47 active customers..."     │            │  │
│  │  │ 10:30 AM            🔊 Speaking       │            │  │
│  │  └──────────────────────────────────────┘            │  │
│  │                                                       │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  🎤 Listening... Speak now                           │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Input Area                                          │  │
│  │  [🎤] [Type or speak your message...] [Send ✈️]     │  │
│  │  💡 Click 🎤 to speak or Press Ctrl+Enter to send   │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

---

**Understanding these flows will help you:**
- Debug issues faster
- Customize behavior
- Optimize performance
- Extend functionality
- Integrate with other features

**Refer to these diagrams when:**
- Planning customizations
- Troubleshooting errors
- Explaining to team members
- Documenting your implementation
